[workspace]
members = ["gbmul-core", "gbmul-wasm", "gbmul-sdl2"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "unwind"
debug = 1  # keep line info for backtraces during debugging

